# MediaWiki Environment Configuration Example
# Copy this file to .env and customize for your environment

# Server Configuration
MW_SERVER=http://localhost:8080
MW_SCRIPT_PATH=/w
MW_DOCKER_PORT=8080

# Database Configuration (for Docker/SQLite)
MW_DBTYPE=sqlite
MW_DBPATH=/var/www/html/w/cache/sqlite

# Admin User Configuration
MEDIAWIKI_USER=Admin
MEDIAWIKI_PASSWORD=dockerpass

# Development Settings
MW_LANG=en
MW_SITENAME=MediaWiki

# Docker User Configuration
# Linux/Mac users: These should be set to your user ID and group ID
# Windows users: Leave these empty
MW_DOCKER_UID=1000
MW_DOCKER_GID=1000

# Debugging Configuration
XDEBUG_CONFIG=
XDEBUG_ENABLE=true
XHPROF_ENABLE=true

# Logging
MW_LOG_DIR=/var/www/html/w/cache

# Deployment Configuration (for deploy script)
# Uncomment and configure for your deployment environment

# Staging Environment
#STAGING_DEPLOY_HOST=staging.example.com
#STAGING_DEPLOY_PATH=/var/www/mediawiki
#STAGING_URL=https://staging.example.com

# Production Environment
#PROD_DEPLOY_HOST=production.example.com
#PROD_DEPLOY_PATH=/var/www/mediawiki
#PRODUCTION_URL=https://example.com

# Deployment User
#DEPLOY_USER=deploy

# Backup Configuration
#BACKUP_DIR=backups

# CI/CD Configuration
# These are typically set in GitHub Secrets, not in .env
#DEPLOY_SSH_KEY=
#SLACK_WEBHOOK_URL=
#NOTIFICATION_EMAIL=
